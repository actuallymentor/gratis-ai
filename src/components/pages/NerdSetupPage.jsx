/**
 * Legacy re-export — NerdSetupPage is now CloudSetupPage.
 *
 * Kept for backward compat with the /nerd-setup route.
 * Defaults to the OpenRouter provider when no ?provider= param is present.
 */
export { default } from './CloudSetupPage'
